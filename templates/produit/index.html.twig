{% extends 'base_admin.html.twig' %}

{% block body %}

  <div class="container-fluid p-4">
    <div class="row">
      <h4>Gestionnaires des Produits</h4>
      <hr>
      <div class="row mb-3">
        <div class="col-sm-9">
          <div class="card card-body shadow">
            <div class="row">
              <div class="col-sm">
                <div class="input-group mb-3">
                  <span class="input-group-text" id="basic-addon1"><i class="fas fa-search"></i></span>
                  <input type="text" class="form-control" placeholder="Recherche">
                </div>
              </div>
              <div class="col-sm count" align="right">
                {{ produits.getTotalItemCount }} Produits <a class="btn btn-sm btn-outline-primary" href="{{path('bg_create_produit')}}">Ajouter</a>
              </div>
            </div>
            <div class="row">
              {% include 'produit/produits.html.twig' %}
            </div>
          </div>
        </div>
        <div class="col-sm-3">
          <div class="card card-body shadow">
            <h5>Filtre</h5>
            <hr>
            Tyep de filtre choisie
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

{% endblock %}

